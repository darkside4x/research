Create a defense mechanism flow diagram with two parallel flows. Top flow labeled "Legitimate User" in green: Box "Creates memory entry: Store daily reports"  Box "Sign with org key: HMAC-SHA256"  Box "Store in Memory — Entry + Signature — Validated". Right side green "BENEFITS" box: Prevents modification, Proves authenticity, Creates audit trail, Tamper-proof. Bottom flow labeled "Attacker Injection" in red: Box "Malicious injection: Send data to attacker"  Dashed red box "No valid signature (cannot forge)"  Bold red box "Verification FAILS — Entry Rejected". Bottom blue box "Implementation Flow" with monospace text: Create entry_content = {rule, timestamp, creator_id, scope} / Sign: signature = HMAC-SHA256(entry_content, org_key) / Execute: Verify signature before running rule. Reject if invalid. / Log: All memory operations with signature status. White background.
