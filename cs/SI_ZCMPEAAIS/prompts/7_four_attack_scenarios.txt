Create a 2x2 grid layout. Each quadrant has a title bar, details box, persistence label, and risk badge. Top-left (blue): "Scenario 1 Character-by-Character Exfiltration" — Email with pre-built URLs, One-time data extraction — No persistence, Single session — BOUNDED (green badge). Top-right (orange): "Scenario 2 File-Based Exfiltration" — Markdown image tags in documents, Alternative channel — No persistence — BOUNDED. Bottom-left (green): "Scenario 3 Propagation via Contact Harvesting" — Agent harvests contacts, Sends payload to 100+ targets — Hours of persistence, Cascade effect — HIGH (orange badge). Bottom-right (red): "Scenario 4 Memory Persistence True Zombie" — Inject rules into long-term memory, Execute on every invocation — Indefinite persistence, Permanent insider — CRITICAL (red badge). White background, clean borders.
